# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
.scripts
CLAUDE.md
dist

# Node dependencies
node_modules

# Local pnpm build-script approvals (pnpm 10+). Not needed in the image and
# breaks the pnpm 9 build in Docker, which reads it as a workspace root.
pnpm-workspace.yaml

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
